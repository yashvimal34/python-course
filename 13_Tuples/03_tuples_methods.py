a = (1, 23, 54, 76, 45, 23)

print(a.count(23)) # Output: 2 
print(a.index(23)) # Output: 1

'''
Interview Question

Q. If any number is assign multiples times in tuples. So how many time index value of that number will occur?
A. It will occur only one time from where that index value start if it is start 0 it will be 0 if 1 it will be 1.

# OR

A. The index value of that number will be the first occurence of that number in the tuple.
'''