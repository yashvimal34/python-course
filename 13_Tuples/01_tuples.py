# Note:
# 1. Tuples are created in ( Small Bracket ).
# 2. Lists are created in [ Big Bracket ].

# Single element tuprle also be created but (comma required).

a = (22, 44, 65, 76, 89)
print(a)
print(type(a))

b = (34,)
print(b)
print(type(b))


'''
Interview Question.
Que. Can single tuple be created?
Ans. Yes, single tuple be created but (comma required).
'''