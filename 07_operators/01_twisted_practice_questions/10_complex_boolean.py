x = 5
y = 10
print(not ((x * 2 == y) or (x + y > 20))) # False

# Nested Parenthesis Equation solved by pedmas
# (not ((x * 2 == y) or (x + y > 20)))
# (not (True or (x + y > 20)))
# (not (True or False))
# (not (True))
# False